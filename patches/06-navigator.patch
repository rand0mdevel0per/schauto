--- a/third_party/blink/renderer/core/frame/navigator.cc
+++ b/third_party/blink/renderer/core/frame/navigator.cc
@@ -1,3 +1,4 @@
+#include "third_party/blink/renderer/core/privacy/fingerprint_toolkit.h"

 // webdriver() → always false
-  return GetFrame()->GetSettings()->GetWebDriverEnabled();
+  return false;

 // hardwareConcurrency()
-  return SysInfo::NumberOfProcessors();
+  auto* tk = GetFrame()->GetFingerprintToolkit();
+  return tk ? tk->SpoofHardwareConcurrency() : 4;

 // platform()
-  return platform_;
+  auto* tk = GetFrame()->GetFingerprintToolkit();
+  return tk ? tk->GetPlatform() : platform_;

 // language()
-  return GetAcceptLanguages();
+  auto* tk = GetFrame()->GetFingerprintToolkit();
+  return tk ? tk->GetLanguage() : GetAcceptLanguages();

 // doNotTrack()
-  return do_not_track_;
+  return "1";
